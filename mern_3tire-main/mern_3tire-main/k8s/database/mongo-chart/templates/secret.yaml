apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  root-username: {{ .Values.secret.rootUsername }}
  root-password: {{ .Values.secret.rootPassword }}
  mongo-auth-db: {{ .Values.secret.authDatabase }}
